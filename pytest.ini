[pytest]
# Restrict collection to tests/: scripts/ holds diagnostic scripts that run
# pipeline work at import time, which collection alone would execute.
testpaths = tests
norecursedirs = scripts training data models assets docs evaluation
